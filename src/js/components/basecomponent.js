class BaseComponent {
    
    constructor(events) {
        /*
        BaseComponent
        У класса BaseComponent должен быть метод constructor. 
        Он принимает на вход массив обработчиков событий и вызывает 
        */
    } 

    _setHandlers() {
        /*
        приватный метод _setHandlers. 
        Этот метод добавляет обработчики конкретным элементам.
        */
    }

}